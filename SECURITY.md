# 安全说明

FinCredit Copilot 是使用合成数据的金融科技演示项目，不是持牌金融机构的生产授信系统。

## 报告安全问题

请不要通过公开 Issue 披露密钥、个人信息或可直接利用的漏洞。请使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告入口，并说明受影响版本、复现步骤、影响范围和建议修复方式。

## 密钥与演示数据

- 真实 API Key 只能保存在未被 Git 跟踪的 `.env` 中；`.env.example` 只保留空值或本地演示默认值。
- `data/demo-documents/` 中的姓名、号码、地址、收入和流水均为合成演示内容，不应替换成真实客户材料后提交到公开仓库。
- 开启公网访问前，必须关闭 `AUTH_DISABLED`，配置受信任来源、TLS、密钥托管、最小权限账号、日志脱敏和数据保留策略。
- 默认账号和密码只适用于隔离的本地演示环境，不能用于生产部署。

## 依赖与扫描

提交前应运行前端审计、后端依赖审计、单元测试和密钥扫描。当前后端的 MLflow 依赖链可能暂时约束 `cryptography` 版本；公开部署前应以最新兼容的 MLflow 版本重新评估并消除已知依赖告警。

## 支持范围

本项目不提供真实授信决策、监管解释或法律意见。页面中的规则阈值、风险建议和模拟审批结果均需要有权限的人工人员复核。
