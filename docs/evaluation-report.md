# P0 评估说明

## 数据集

`evaluations/datasets/fincredit_policy_pilot.json` 包含 30 个政策问答样本：10 个单段直接问题、10 个跨段组合问题、5 个过期/冲突问题、5 个无答案问题。

## 对比方案

- A：纯向量检索。
- B：向量 + 关键词混合检索，并以 RRF 融合。
- C：B 的基础上增加一次受控查询改写。

脚本 `evaluations/run_policy_rag_eval.py` 输出 Recall@5、MRR、引用正确率、无答案 F1、P95 延迟、实际改写 token 和平均检索轮次。

## 当前结果状态

本仓库不预填或伪造线上模型指标。真实 A/B/C 指标必须在以下条件满足后生成：

1. `.env` 中填写有效的百炼 `DASHSCOPE_API_KEY`。
2. PostgreSQL + pgvector 已启动，迁移与政策入库已完成。
3. 能访问百炼免费额度内的文本与 embedding 模型。

运行后产生的 JSON 报告默认不提交仓库，以避免把某次临时环境的延迟和配额状态包装为稳定结论。

## P0 质量闸门

- 过期政策不能作为当前结论。
- 无答案问题必须拒答或转人工，不能编造引用。
- 查询改写不得超过一次。
- 每条政策回答必须有可追踪 citation_id、官方 URL、版本和生效日期。
- DTI/LTV 必须由确定性代码计算，且结果必须要求人工复核。
- 429、超时、连接失败必须结构化降级并带 trace_id。
